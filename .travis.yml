sudo: required
language: node_js

services:
  - docker

before_install:
  - docker build -t jakesmoon/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jakesmoon/docker-react npm run test